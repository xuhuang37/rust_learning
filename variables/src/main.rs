fn main() {
    let x = 1;
    let x = x + 1;
    let x = x * 2;
    println!("The value of x is:{}",x);

    let space = "    ";
    let space = space.len();
    // let space = "    ";
    // space = space.len();

    println!("the length of space : {}",space);
}
